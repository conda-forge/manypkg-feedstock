context:
  version: "0.25.0"

package:
  name: manypkg
  version: ${{ version }}

source:
  url: https://registry.npmjs.org/@manypkg/cli/-/cli-${{ version }}.tgz
  sha256: 5782acf5139d37e94a4d23703c41dfae1e5f930dda7a9e3725569cc624dc8fc5

build:
  number: 0
  noarch: generic

requirements:
  build:
    - nodejs
    - pnpm
    - pnpm-licenses
  run:
    - nodejs

tests:
  - script:
      - "manypkg 2>&1 | grep -q \"command undefined not found, only check, exec, run, upgrade, npm-tag and fix exist\""

about:
  license: MIT
  license_file:
    - LICENSE
    - third-party-licenses.txt
  summary: An umbrella for your monorepo
  homepage: https://github.com/Thinkmill/manypkg
  repository: https://github.com/Thinkmill/manypkg

extra:
  recipe-maintainers:
    - danielnachun
